import deployed from '../../../contracts/deployed.json';

export const CONTRACT_ADDRESS = deployed.address;
export const CONTRACT_ABI = deployed.abi;
export const SHARDEUM_RPC = "https://api-mezame.shardeum.org";
